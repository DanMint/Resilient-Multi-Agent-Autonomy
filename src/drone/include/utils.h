#pragma once

#include <iostream> 
#include <unordered_map>

class Utils {
    public:
        static void checkValidityOfNetwork(const std::unordered_map<std::string, std::string> &ips, const std::string &hostName);

    private:

};